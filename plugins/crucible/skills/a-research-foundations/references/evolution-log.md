# evolution-log: a-research-foundations

Append-only. Newest entries on top.

Each entry: date, trigger (what happened), change (what was edited), evidence (link to audit folder / session ID).

---

## 2026-04-19 — Initial creation

**Trigger**: Session `ca9d7ffa-ef5a-41d0-94c8-56f113a132f2`. Consolidated 6 principles from `findings/methodology/` + gap-analysis into this skill.

**Change**: Created `SKILL.md` with frontmatter + 6 principle sections + confirmation-count table + post-execution reflection.

**Evidence**: `findings/evolution/audits/2026-04-19-ngram3fu-straddle/CHRONICLE.md`, individual `findings/methodology/*.md` files.

**Confirmations so far**: see "Confirmation counts" table in parent `SKILL.md`.

**Gaps to test** (filled on first re-invocation):

- Does each principle's guidance hold on a DIFFERENT research hypothesis (not just NGRAM3FU-STRADDLE)?
- Are confirmation counts incremented / decremented as new evidence arrives?
